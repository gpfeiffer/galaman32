<p id="notice"><%= notice %></p>

<h3>
  <%= @event.competition.name %>
(<%= @event.competition.date %>,
<%= @event.competition.location %>)
</h3>

<h1>
  Event <%= @event.pos %>.
  <%= @event.discipline.to_words %>
</h1>
<h4>
  <%= ages_to_words(@event.age_range) %>
</h4>

<h2>Results.</h2>

<% @event.qualification_age_ranges.each do |ages| %>
  <% results = @event.listed_results(ages) %>
  <% if not results.empty? %>

    <hr />
    <h3><%= ages_to_words(ages) %></h3>

    <table class="fact-sheet">
      <tr class="fact-major">
        <th style="width:50px">Plc</th>
        <th style="width:175px">Name</th>
        <th>Age</th>
        <th>Team</th>
        <th style="width:75px">Result</th>
        <th></th>
      </tr>
      <%= render results %>
    </table>

  <% end %>
<% end %>

<br />
<hr />

<%= render 'events/navigation' %> 
| <%= link_to 'Print', results_path(:event_id => @event, :format => :tex), :target => "_blank" %> 
