<h1>Listing heats</h1>

<table>
  <tr>
    <th>Pos</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @heats.each do |heat| %>
  <tr>
    <td><%= heat.pos %></td>
    <td><%= link_to 'Show', heat %></td>
    <td><%= link_to 'Edit', edit_heat_path(heat) %></td>
    <td><%= link_to 'Destroy', heat, :confirm => 'Are you sure?', :method => :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Heat', new_heat_path %>
