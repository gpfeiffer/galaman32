<tr>
  <td align="right"><%= swimmer.registration %></td>
  <td><%= link_to swimmer.name, swimmer %> [<%= swimmer.age %>]</td>
<% if session[:edit] %>
  <td><%= link_to image_tag('delete.png', :class => "icon"), swimmer, :confirm => 'Are you sure?', :method => :delete %></td>
<% end %>
</tr>
