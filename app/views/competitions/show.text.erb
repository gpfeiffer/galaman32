<%= @competition.name %> (<%= @competition.date %>)

<% Swimmer::GENDERS.each do |gender| %>

<%= @club.name %> <%= { 'f' => 'Girls', 'm' => 'Boys' }[gender] %>.

<%= sprintf("%-20s  %-10s  % 10s", 'Name', 'Birthday', 'SI Member') %>

<%= render @competition.registrations.select{ |x| x.swimmer.gender == gender }.sort_by { |x| x.swimmer.name } %>

<% end %>
