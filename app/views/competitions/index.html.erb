<h1>Competitions</h1>

<table>
<% @competitions.reverse_each do |competition| %>
  <tr>
    <td><%= link_to competition.name, competition %>
    (<%= competition.location %>,
    <%= competition.date %>)</td>
    <td><%= link_to image_tag('delete.png', :class => "icon"), competition, :confirm => 'Are you sure?', :method => :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Competition', new_competition_path %>
