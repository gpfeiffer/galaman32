<h1>Clubs</h1>

<table>
<% @clubs.each do |club| %>
  <tr>
    <td><%= link_to club.name, club %></td>
    <td>:</td>
    <td align="right"><%= club.swimmers.count %> swimmers</td>
<% if session[:edit] %>
    <td><%= link_to image_tag('delete.png', :class => "icon"), club, :confirm => 'Are you sure?', :method => :delete %></td>
<% end %>
  </tr>
<% end %>
</table>

<br />
<hr />

<% if session[:edit] %>
<%= link_to 'New Club', new_club_path %>
<% end %>
