\subsection*{Event <%= event.pos %>. <%= event.discipline.to_words %>}

\begin{center}
\tablefirsthead{}
\tablehead{}
\tabletail{}
\tablelasttail{}
<%# FIXME: prepare this table in controller/model ...%>
<% times = event.qualification_times %>
<% if times.any? %>
<% names = times.map { |x| x.qualification.name }.uniq %>
<% ranges = times.map { |x| x.age_range }.uniq %>
\begin{supertabular}{|l<%= 'r' * names.count %>|}
\toprule
Age Range 
<% names.each do |name| %>
& <%= name %>
<% end %> \\
\midrule
<% ranges.each do |range| %>
<%= ages_to_words(range) %>
<% names.each do |name| %>
& <%= times.select { |x| x.age_range == range and x.qualification.name == name }[0] %>
<% end %> \\
<% end %>
\bottomrule
\end{supertabular}
\end{center}
<% end %>

\begin{center}
\tablefirsthead{%
\toprule
& {\footnotesize Name, Age} & {\footnotesize Team} & \hbox to 15mm{}\llap{\footnotesize Seed Time} & {\footnotesize Result} \\
\midrule%
}
\tablehead{%
\toprule
\multicolumn{4}{l}{\textbf{\ldots\ Event <%= event.pos %>. <%= event.discipline.to_words %>}}\\
\midrule%
& {\footnotesize Name, Age} & {\footnotesize Team} & \hbox to 15mm{}\llap{\footnotesize Seed Time} & {\footnotesize Result} \\
\midrule%
}
\tabletail{%
&&&&\ldots\\
}
\tablelasttail{%
\bottomrule%
}
\begin{supertabular}{r@{ }p{35mm}p{8mm}rc}
<% event.seeded_entries.each_with_index do |entry, index| %>
  <%= render :partial => 'events/entry', :object => entry, :locals => { :index => index } %>
<% end %>
\end{supertabular}
\end{center}
