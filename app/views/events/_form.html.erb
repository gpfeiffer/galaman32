<%= form_for(@event) do |f| %>
  <% if @event.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@event.errors.count, "error") %> prohibited this event from being saved:</h2>

      <ul>
      <% @event.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <%= f.hidden_field :competition_id %>

  <h3><%= @event.competition.name %></h3>

  <div class="field">
    <%= f.label :discipline_id %><br />
    <%= f.select :discipline_id, Discipline.all.map {|x| [x.name, x.id]} %>
  </div>
  <div class="field">
    <%= f.label :pos, "Position" %><br />
    <%= f.text_field :pos %>
  </div>
  <div class="field">
    <%= f.label :age_min, "Minimal Age" %><br />
    <%= f.text_field :age_min %>
  </div>
  <div class="field">
    <%= f.label :age_max, "Maximal Age" %><br />
    <%= f.text_field :age_max %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
